// DashboardPreview removed — replaced by Features section which describes
// only what is actually built. No fake metrics or mock charts.
// This file is kept as an empty export to avoid import errors in Index.tsx.

export function DashboardPreview() {
  return null;
}
