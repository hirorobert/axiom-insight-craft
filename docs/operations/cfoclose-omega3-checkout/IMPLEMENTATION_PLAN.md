# Ω3-CHECKOUT — Implementation Plan

Design document only. Nothing below has been applied. Every file is marked MUST CHANGE / MAY CHANGE / MUST NOT CHANGE for a future, separate implementation pass — subject to the independent Codex audit this package exists to enable.

**Design-correction round 1:** the resolver deployment is now explicitly three-phase (schema → application → cleanup), `commit_verified_commercial_payment` moves from MUST NOT CHANGE to MUST CHANGE (a customer-level lock is required — item 6), `admin_upsert_commercial_offer` gains the same product-scoping fix as the resolver, the two USD offers are seeded non-purchasable with a separate activation step (item 12), `list_purchasable_commercial_offers` is withdrawn entirely (item 9 — no proven caller), and every "renewal" reference means manual, customer-initiated renewal (item 8).

**Design-correction round 2 (this revision):** round 1's `resolve_commercial_offer` product-scoping relied on `auth.uid()`, which is `NULL` under the service-role client `commercial-create-checkout` actually uses — split into `resolve_commercial_offer` (display) and a new, service-role-only `resolve_commercial_checkout_offer` (checkout, item 1). The single-RPC `acquire_or_reuse_checkout_intent` from round 1 is withdrawn and replaced by a three-RPC, token-fenced lease protocol (item 3). `admin_supersede_commercial_offer` cannot activate/deactivate an existing offer row in place — a new, narrow `admin_set_offer_purchasable` RPC is added (item 4). `admin_upsert_commercial_offer` gains an explicit product-code parameter as its own new overload, on top of round 1's product-scoping fix (item 5). The webhook `chk_pwpe_result` CHECK constraint gets exact `DROP`/`ADD` DDL for four new values (item 6). Reconciliation is committed to one mechanism — `pg_cron` + `pg_net` + a `SKIP LOCKED` claim RPC — not left as an open choice (item 7). The platform-state gate is corrected to check the actually-selected provider and to use a single, fully-specified restricted-identity mechanism (item 8). GLOBAL-only checkout's effect on Tanzania offers is stated explicitly with a required pre-launch proof query (item 9). Every new function's exact privilege grant is now specified (item 2) — cross-referenced from `COMMERCIAL_ARCHITECTURE_AUDIT.md` §8's grant table, not repeated in full here.

## 1. File-level slices

### MUST CHANGE

| File | Change |
|---|---|
| `supabase/migrations/<new timestamp A>_omega3_checkout_resolver_and_lease.sql` (new file, **Phase A**) | Creates the 3-arg `resolve_commercial_offer` (display, `DATA_CONTRACTS.md` §1a) **alongside** the still-live 2-arg overload; creates `commercial_default_product_id()`; creates the NEW service-role-only `resolve_commercial_checkout_offer` (§1b, item 1 round 2); creates the three lease-protocol RPCs `acquire_checkout_intent_lease`, `persist_checkout_provider_result`, `fail_checkout_intent_lease` (§3, item 3 round 2, replacing round 1's withdrawn single-RPC design) plus the `payment_checkout_intents` schema additions (`creation_token`, `lease_expires_at`, the additive `PROVIDER_CREATING` status value); creates `admin_set_offer_purchasable` (§4, item 4 round 2); adds the customer-level lock to `commit_verified_commercial_payment` (`CREATE OR REPLACE`, same signature — a true replace); fixes `admin_upsert_commercial_offer`'s unscoped plan lookup AND adds `p_product_code` as a new 12-arg overload alongside the still-live 11-arg one (§5, item 5 round 2); adds the four new `chk_pwpe_result` values (§6, item 6 round 2) via explicit `DROP`/`ADD CONSTRAINT`; adds the reconciliation schema (`reconciliation_claimed_at`, `reconciliation_attempt_count` on `payment_checkout_intents`) and the `claim_stale_checkout_intents_for_reconciliation` RPC (§7, item 7 round 2). Exact grants for every function in this migration are specified in `COMMERCIAL_ARCHITECTURE_AUDIT.md` §8's table — every one is either `service_role`-only or `authenticated`+`is_commercial_admin()`-gated, none is left ungranted or ambiguously scoped. |
| `supabase/functions/commercial-create-checkout/index.ts` (**Phase B**, deployed only after Phase A migration is live) | Full corrected flow per `DATA_CONTRACTS.md` §2: `marketCode` removed from the request body (unchanged from round 1); platform-state gate Part A (provider-independent) runs after auth; `billing_customer_id` derived from the validated `user.id` via a direct table lookup (item 1); restricted-acceptance-identity resolved via a direct `commercial_admins` lookup on that same validated id (item 8); offer resolved via the new `resolve_commercial_checkout_offer`; platform-state gate Part B (provider-environment mismatch) runs AFTER `selectPaymentProvider`, checking the actually-selected provider's capability entry, never a fixed array index (item 8's confirmed bug fix); checkout-intent creation goes through the three-RPC lease protocol, never a direct `INSERT` or the withdrawn single-RPC design (item 3) |
| `supabase/functions/commercial-payment-webhook/index.ts` | Gate B failure handling gains the transient/definitive split (round 1, item 7): a network/5xx/timeout contacting Flutterwave's verify endpoint records `VERIFICATION_TRANSIENT_FAILURE` and returns 500 (not 200) to trigger Flutterwave's own redelivery, distinct from a definitive mismatch/forgery rejection |
| `supabase/functions/commercial-payment-reconcile/index.ts` (**new Edge Function**, item 7 round 2) | Invoked on a schedule by `pg_cron`/`pg_net`; calls `claim_stale_checkout_intents_for_reconciliation`, then for each claimed row calls the adapter's new `verifyTransactionByReference` and, on a verified success, the identical `authoriseCommit` + `commit_verified_commercial_payment` path a webhook would use. Never a lesser-authority shortcut. |
| `supabase/functions/_shared/payments/providers/flutterwave.ts:147-155` | Stale-branding fix, unchanged from round 1 (`DATA_CONTRACTS.md` §9 in round 1's revision, now folded into this document's §9 reference). **New this round:** add `verifyTransactionByReference(saffReference, expectedMinor, expectedCurrency)`, calling Flutterwave's `GET /transactions?tx_ref=...` lookup-by-reference endpoint and running the result through the same amount/currency/reference checks `verifyTransaction` already performs — required by the reconciliation mechanism (item 7), since reconciliation by definition has no `providerTransactionId` yet. |
| `supabase/functions/_shared/payments/routing.ts` | No structural change, but `getConfiguredProviders()` is now also consulted post-selection by `commercial-create-checkout`'s corrected platform-state gate (item 8) — `.find(p => p.provider === selectedProvider)` rather than `[0]`. No change to `routing.ts` itself; noted here because its exported shape is what makes the corrected lookup possible without any new field. |
| `src/lib/commercial/commercialRpc.ts` | `createCheckoutIntent(planCode, billingInterval)` — unchanged from round 1 |
| `src/components/commercial/CheckoutUpgradeButton.tsx` | Unchanged from round 1 — a 409 `CHECKOUT_IN_PROGRESS` response (item 3's `LEASE_HELD` case) is handled the same way any other checkout-initiation error is handled today (a toast, button re-enabled), no new UI state required |
| `src/pages/Pricing.tsx` | Unchanged from round 1 |
| Test files: new/updated tests for both correction rounds' mechanisms | Follow the established static-source-text regression-guard convention. New round-2 additions: privilege-denial tests for every `service_role`-only function (`ACCEPTANCE_MATRIX.md` §8), lease-race and CAS tests, the `chk_pwpe_result` constraint's new values, `SKIP LOCKED` disjoint-claim proof, and the corrected provider-selection platform-state check |
| `supabase/migrations/<new timestamp C>_omega3_checkout_resolver_cleanup.sql` (new file, **Phase C**, deployed only after Phase B is confirmed live and stable) | `REVOKE`+`DROP` the old 2-arg `resolve_commercial_offer(TEXT,TEXT)` overload AND the old 11-arg `admin_upsert_commercial_offer` overload (item 5 round 2 adds this second cleanup target to the same phase, since both follow the identical three-phase discipline) |

### CONTROLLED DATA-SEED OPERATION (not a schema migration — see §3) — corrected for non-purchasable seeding (item 12) and the corrected activation RPC (item 4, round 2)

| Action | Detail |
|---|---|
| Step 1 — create two authoritative USD offers, **non-purchasable** | `admin_upsert_commercial_offer('CFOCLOSE-PAID-GLOBAL-USD-MONTHLY', 'SAFF_ERP', 'PAID', 'GLOBAL', 'USD', 4900, 2, 'MONTHLY', 1, true, false, '<reason>')` (the new 12-arg overload with `p_product_code`, §5 round 2) and the ANNUAL equivalent (49900, `false`) — `is_active=true` but `is_purchasable=false` |
| Step 2 — activation, a SEPARATE, later, explicitly audited operation | Only after every gate in `ACCEPTANCE_MATRIX.md` passes on staging: `admin_set_offer_purchasable(offer_code, false, true, '<reason>')` (§4, round 2 — **not** `admin_supersede_commercial_offer`, which round 2's independent review confirmed cannot toggle an existing row's flag: it only creates a new successor row, and has no path to flip the same row's flag back to `false` for a rollback either) flips `is_purchasable = true` for each offer, one at a time, with expected-state (compare-and-swap) protection, each independently reversible via the same function and independently audited via `commercial_catalog_audit_events`. Step 2 must never be bundled into the same deployment or the same admin call as Step 1. |

### MAY CHANGE

| File | Reason |
|---|---|
| `src/lib/commercial/payments/{paymentTypes.ts,paymentAuthority.ts}` and their 4 test files | Unchanged from the prior round — shadow coverage, not required to ship |
| `src/constants/copy.ts` (`PRICING` block) | Only if the parity mechanism in §5 lives here |

### MUST NOT CHANGE

- `supabase/functions/commercial-payment-webhook/index.ts`'s Gate A/replay/receipt model — **unchanged except** the Gate B transient/definitive split (item 7), which is a narrow, additive change to how one failure category is classified and responded to, not a redesign of the webhook flow.
- `supabase/functions/_shared/payments/authority.ts` (`authoriseCommit`) — unchanged.
- `supabase/functions/_shared/payments/money.ts`, `contracts.ts`, `routing.ts` — unchanged.
- `record_payment_reversal`, `get_checkout_status`, `admin_get_billing_detail` — unchanged.
- `commercial_licences`, `entitlement_overrides`, `_resolve_entitlement_for_owner`, `get_effective_entitlement` — unchanged; entitlement is plan-scoped, not interval- or lock-granularity-scoped.
- Any RLS policy — unchanged.
- `commercial_offers` table schema, `uq_co_current_offer`, `excl_co_no_overlapping_purchasable_periods` — unchanged (still sufficient, §5 of the architecture audit).
- `src/lib/workspace/*`, `stageMetadata.ts`, SAFISHA/HESABU/KINGA/MAONO — untouched.
- `src/pages/billing/PaymentReturn.tsx` — unchanged; already displays whatever `get_checkout_status` returns.

### Corrected across both design-correction rounds

| Item | Prior classification | Corrected classification | Why |
|---|---|---|---|
| `commit_verified_commercial_payment` | MUST NOT CHANGE (original) | **MUST CHANGE** (round 1) | Item 6 requires a customer-level lock. Unchanged by round 2. |
| `list_purchasable_commercial_offers` (new function) | Proposed as MUST CHANGE (original) | **Withdrawn entirely** (round 1) | Item 9 (round 1) — no proven caller. |
| USD offer seeding | Single-step creation with `is_purchasable=true` (original) | **Split into create (non-purchasable) + separately-audited activation** (round 1) | Item 12 (round 1). |
| `resolve_commercial_offer`'s product scoping | Single function, `auth.uid()`-based (round 1) | **Split into `resolve_commercial_offer` (display, `auth.uid()`) and `resolve_commercial_checkout_offer` (checkout, service-role-only, explicit `billing_customer_id`)** | Item 1 (round 2) — round 1's fix relied on `auth.uid()`, which is `NULL` under the service-role client `commercial-create-checkout` actually uses; the fix would never have activated at its real call site. |
| Checkout-intent reuse logic | Edge-Function `SELECT`-then-reuse (original) → single atomic `acquire_or_reuse_checkout_intent` RPC (round 1) | **Withdrawn; replaced with a three-RPC token-fenced lease protocol** (`acquire_checkout_intent_lease` / `persist_checkout_provider_result` / `fail_checkout_intent_lease`) | Item 3 (round 2) — round 1's single RPC released its advisory lock before the external provider call completed, leaving a window where a live in-flight attempt's row could be wrongly superseded. |
| Offer activation/deactivation | Assumed reachable via `admin_supersede_commercial_offer` (round 1, implicit) | **New, narrow `admin_set_offer_purchasable` RPC with expected-state CAS** | Item 4 (round 2) — `admin_supersede_commercial_offer` only creates new successor rows; it has no path to toggle an existing row's `is_purchasable` flag in either direction. |
| `admin_upsert_commercial_offer` | Product-scoping fix only, no product parameter (round 1) | **New 12-arg overload adds explicit `p_product_code`, STRICT product+plan resolution** | Item 5 (round 2). |
| Webhook `chk_pwpe_result` | New values implied but no DDL given (round 1) | **Exact `DROP`/`ADD CONSTRAINT` DDL specified, 4 new additive values** | Item 6 (round 2). |
| Reconciliation | "An Edge Function or pg_cron" (round 1, unresolved) | **Committed to one mechanism: `pg_cron`+`pg_net` triggering a dedicated Edge Function, `SKIP LOCKED` row claiming, exponential backoff, terminal-failure cap** | Item 7 (round 2). |
| Platform-state gate | Checked `getConfiguredProviders()[0]`; restricted-identity mechanism left as an open choice (round 1) | **Checks the actually-selected provider (post-routing); restricted identity resolved via a direct `commercial_admins` lookup, one mechanism, fully specified** | Item 8 (round 2) — round 1's `[0]` check could validate an unrelated provider's environment. |
| Tanzania/GLOBAL claim | "Nothing in this design touches TZ-specific offers" (round 1) | **Corrected: GLOBAL-only checkout makes TZ checkout categorically unreachable; proven non-regressive only because both existing TZ offers are already non-purchasable, re-verified at deploy time** | Item 9 (round 2) — round 1's claim was true at the schema level but overstated the behavioral effect. |

## 2. Concurrency implementation note

The token-fenced lease protocol (`acquire_checkout_intent_lease` / `persist_checkout_provider_result` / `fail_checkout_intent_lease`, `DATA_CONTRACTS.md` §3) and the customer-level lock inside `commit_verified_commercial_payment` (`DATA_CONTRACTS.md` §11, unchanged since round 1) are both **database-enforced**, not application-level check-then-act logic. The lease protocol itself superseded an intermediate, single-RPC atomic design from round 1 that was correctly atomic for its own transaction but left an unlocked window during the external provider call — see `COMMERCIAL_ARCHITECTURE_AUDIT.md` §6 for why that intermediate design was still insufficient. See `THREAT_AND_FAILURE_MODEL.md` Part 1 for the full scenario-by-scenario re-audit across both rounds.

## 3. Migration safety

- **Phase A migration** (all new functions and schema additions listed in §1's MUST CHANGE table above — the 3-arg `resolve_commercial_offer`, `resolve_commercial_checkout_offer`, the three lease RPCs, `admin_set_offer_purchasable`, the `admin_upsert_commercial_offer` 12-arg overload, the `chk_pwpe_result` DDL, the reconciliation schema and RPC): forward-only, no `DROP TABLE`, no destructive alteration. Both old overloads (`resolve_commercial_offer(TEXT,TEXT)`, `admin_upsert_commercial_offer`'s 11-arg form) are left in place and functioning exactly as before for the duration of this phase — this migration changes zero existing behavior for any in-flight caller.
- **Phase B** (application deploy): the Edge Function and frontend changes, plus the new `commercial-payment-reconcile` function and its `pg_cron` schedule. Rollback of this phase alone is safe — both old overloads remain present and callable, so a reverted Edge Function continues to work exactly as it did before any of this shipped.
- **Phase C migration** (drop both old overloads — `resolve_commercial_offer(TEXT,TEXT)` and `admin_upsert_commercial_offer`'s 11-arg form): deployed only after Phase B is confirmed stable — see the three-phase deployment order in `DATA_CONTRACTS.md` §1 and §5. This is the direct answer to item 1's "define safe migration/Edge-Function deployment order" requirement, applied consistently to both functions that gained a new overload rather than a same-signature replace.
- **Controlled commercial data-seed operation:** unchanged in kind from the prior round (an admin RPC call, not a migration, reversible via supersession, never a `DELETE`) — corrected only in that Step 1 (create) and Step 2 (activate) are now explicitly two separate, independently-audited operations (item 12).
- Pre-deployment verification: the full existing commercial test suite plus every new/updated test in §1's MUST CHANGE row. None of the existing tests assert a fixed resolver arity as a requirement (re-confirmed this round) except `marketPropagation.test.ts`, which is explicitly retired.
- Post-deployment verification: `ACCEPTANCE_MATRIX.md` in full, including the new platform-state, concurrency, product-binding, and manual-renewal-disclosure sections.
- Frontend economics: `Pricing.tsx`'s `PRICING.MONTHLY_USD`/`ANNUAL_USD` constants remain a **display fallback only until the resolver call succeeds** — once checkout is enabled, the page must render the server-resolved amount for the actually-selected interval (item 9), and a detected mismatch between the two must **disable the checkout CTA and raise an acceptance failure**, not merely log a warning. See §5.

## 4. Rollback

| Layer | Rollback action |
|---|---|
| Frontend (`Pricing.tsx`, `CheckoutUpgradeButton.tsx`, `commercialRpc.ts`) | Revert the commit(s); the lock panel can be restored immediately |
| Edge Function (`commercial-create-checkout`) | Revert to the pre-Ω3-CHECKOUT deployed version; safe independent of migration phase (see §3) |
| Flutterwave adapter branding fix | Purely cosmetic; plain text revert |
| Phase A migration | `DROP FUNCTION` every new function introduced in Phase A (the new 3-arg resolver, `resolve_commercial_checkout_offer`, `commercial_default_product_id`, the three lease RPCs, `admin_set_offer_purchasable`, the new `admin_upsert_commercial_offer` 12-arg overload, `claim_stale_checkout_intents_for_reconciliation`); `CREATE OR REPLACE FUNCTION` `commit_verified_commercial_payment` back to its pre-lock body (preserved verbatim in git history); revert the `chk_pwpe_result` `DROP`/`ADD` to its pre-Ω3-CHECKOUT value list |
| Phase C migration | Re-`CREATE FUNCTION` either dropped overload if ever needed (bodies quoted verbatim in this package's git history) — expected never to be necessary once Phase C has shipped, since nothing calls either by then |
| Commercial offers (the two new USD rows) | **Never delete.** If activated (Step 2) and a rollback to non-purchasable is needed, `admin_set_offer_purchasable(offer_code, true, false, '<reason>')` flips `is_purchasable` back to `false` (its expected-state CAS requires stating the current value, `true`, explicitly) — preserving the historical evidence trail. If not yet activated, simply do not perform Step 2. |
| `commercial-payment-reconcile` / `pg_cron` schedule | `SELECT cron.unschedule('omega3_checkout_reconciliation')` stops future runs immediately; already-claimed rows simply age out of their claim window and become reclaimable again if the job is later re-enabled — no data loss, no orphaned lock state (advisory locks and `SKIP LOCKED` row-locks are both transaction-scoped and release automatically) |
| `commercial_platform_state` | Roll back to `PAYMENTS_DISABLED` via `admin_transition_platform_state` for a full pause — this is the gate's designed purpose, and with this design's new enforcement (item 4), doing so now actually stops checkout traffic, unlike before this implementation ships. |

## 5. Public pricing / authoritative offer drift, and the withdrawn listing function (items 9)

**Parity mechanism, corrected to be enforcing, not advisory:** `Pricing.tsx`, once wired to real checkout, must call `resolve_commercial_offer` for both intervals (or lazily for the selected one) and compare the returned `amount_minor`/`currency_code` against `PRICING.MONTHLY_USD`/`ANNUAL_USD`. On a match, render normally. **On a mismatch, disable the checkout CTA and surface a visible "pricing temporarily unavailable" state** — this is the acceptance-failure behavior item 9 requires, replacing the prior round's softer "staging smoke test / admin warning" framing, which did not actually prevent a live mismatch from reaching a customer. This still makes the DB the sole authority (the frontend never invents a number; it only refuses to proceed on disagreement) and still requires no new architecture.

**`list_purchasable_commercial_offers` is fully withdrawn** — not deferred, not "may change." If a genuine future need for an interval-independent listing surface arises (e.g. an admin catalogue UI), it gets its own design pass with a real, named caller identified first.

## 6. Status

No implementation was performed in this pass. This document, together with the other five in `docs/operations/cfoclose-omega3-checkout/`, is the complete, corrected design/audit package.
